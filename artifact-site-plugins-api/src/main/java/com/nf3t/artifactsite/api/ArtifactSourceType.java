package com.nf3t.artifactsite.api;

/**
 * Origin type for parsed artifact inputs.
 */
public enum ArtifactSourceType {
    /** Local filesystem input. */
    LOCAL,
    /** Remote URL input. */
    REMOTE
}
